{{#each posts}}
{{> section-label}}
  {{> all-titles}}
    {{> all-posts}}
{{/each}}

  <input type="hidden" id="relevant" value="{{current}}"/>

{{> script-homepage}}
{{#if logged_In}}
{{> script-logout}}
{{/if}}