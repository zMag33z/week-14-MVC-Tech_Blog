{{> section-label}}
        <h2 id="topic-title">{{viewTitle.posts}}</h2>
    </div>

    <div class="comments underline">
        <p id="{{sectionID}}" class="click-width open">posts:  <span id="comment-total">{{posts.length}}</span></p>
    </div>

    <div class="list">
        <div class="list-top">
            <p id="{{sectionID}}" class="close">-</p>
        </div>

    {{#each posts}}
        <section>
            <div class="section-label" data-style="hover">
            {{> all-titles}}
            {{> all-posts}}

    {{/each}}

{{> close-section}}


{{> section-label}}
        <h2 id="topic-title">{{viewTitle.comments}}</h2>
    </div>

    <div class="comments underline">
        <p id="{{sectionID}}" class="click-width open">comments:  <span id="comment-total">{{comments.length}}</span></p>
    </div>

    <div class="list">
        <div class="list-top">
            <p id="hide-list" class="close">-</p>
        </div>

        <section>
            {{#each comments}}
            {{> all-comment-postTitles}}
            {{> all-comments}}
            {{/each}}
        </section>        
    </div>

{{> close-section}}

{{> new-post}}

{{> script-dashboard}}
{{> script-logout}}