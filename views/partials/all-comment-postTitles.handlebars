            <div class="section-label" data-style="userHov">
                <h2 id="{{post.id}}" class="topic-title">{{post.title}}</h2>
                <p id="by-user">Posted by: {{post.user.name}} on {{post.post_date}}</p>
            </div>

            <div class="posts">
                <p id="user-post">{{post.post_text}}</p>
            </div>