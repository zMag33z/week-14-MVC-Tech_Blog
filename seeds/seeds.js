const sequelize = require('../config/connection');
// require class objects to extend to sequelize model class

// require json data for seeding 

// const seedDatabase = async function await sequelize bulkcreate for each create one 

// process.exit(0); til server ready for connection

// seedDatabase();  init function when file called.